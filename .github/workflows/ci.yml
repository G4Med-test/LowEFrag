name: CI

on:
  push:
    branches: [ master ]
  pull_request:
  workflow_dispatch:

jobs:
  build-apptainer:
    uses: G4Med-test/ci-workflows/.github/workflows/apptainer-build-deploy.yml@master
    with:
      apptainer_def: "container/LowEFrag.def"
      image_name: "LowEFrag.sif"
